export class Music {
  constructor({address, begin_time, alt, fee_str, image, content, title}) {
    this.address = address
    this.begin_time = begin_time
    this.alt = alt
    this.fee_str = fee_str
    this.image = image
    this.content = content
    this.title = title
  }
}
